{
  "name": "upstash-redis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsc --watch -p tsconfig.json",
    "build": "tsc -p tsconfig.json && cp package*.json build && cd build && npm ci --production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.115",
    "@types/node": "^20.2.5",
    "aws-lambda": "^1.0.7",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "ioredis": "5.3.2"
  }
}
